import styles from './Footer.module.css'

const Footer = () => (
  <div className={styles.footer}>
      <p>This is the footer, where links to a bunch of stuff can go</p>
  </div>
)

export default Footer